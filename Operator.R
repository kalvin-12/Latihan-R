#Operator Penugasan/penjelas => (=,<-,->) 
x = 20
y <- 5
10 -> z

#Operator Aritmatika => (+,-,*,/,^,%%,%/%)

#hasil = x * z
#hasil = z^2
hasil = z-y*x

#Operator Relasi => (>,<,>=,<=,==,!=) TRUE/FALSE
#Operator relasi digunakan untuk membandingkan satu objek dengan objek lainnya

#hasil <- x < y
Relasi <- z != y
#Relasi <- z == y FALSE

#Operator Logika => (&&,!,||,&) 
#Semua angka bernilai 1 akan dianggap bernilai logika TRUE.
# berlaku pada vektor dengan tipe logical, numeric, atau complex


y >= 1 && z <= 10 -> Logika
